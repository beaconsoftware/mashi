{
  "name": "mashi-dxt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "bundle": "pnpm build && cd .. && rm -f mashi.dxt && (cd dxt && zip -r ../mashi.dxt manifest.json dist icon.png 2>/dev/null || zip -r ../mashi.dxt manifest.json dist)"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0"
  }
}
